/** @type {import('next').NextConfig} */
// Minimal Next.js App Router config for the human UX layer (CHUNK_1_AUTH bootstrap).
// The pipeline (src/**) and its vitest/tsc/eslint gate are untouched by this app.
const nextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
