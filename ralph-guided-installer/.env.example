# No new environment variables extracted from spec — this feature reuses ap-hub's existing
# .env (DATABASE_URL, QBO_ENV, GOOGLE_SSO_CLIENT_ID/SECRET, SESSION_COOKIE_SECRET, etc.).
# See the ap-hub repo root .env.example for the full list.
