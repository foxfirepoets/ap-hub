# Implementation Plan — Windows Local-Only Runtime

Tasks are ordered by dependency. Each checkbox is one independently committable
continuous-build-verify task and must finish with `npm run verify` unless the task
also names an installed-environment validation.

## CHUNK_1_SECRETS — Windows Credential Manager authority

- [x] Add `migrations/013_local_runtime_credentials.sql` and `.down.sql` for `credential_refs` plus constrained `connections.transport_mode`/`transport_config`; extend `src/db/migrate.ts` retained-row DOWN refusal and `test/accounting-intake-migration.test.ts` UP → DOWN → UP/schema/rollback coverage.
- [x] Extend `src/host/types.ts` with the `SecretStore`/credential-target interfaces and implement `WindowsCredentialManagerSecretStore` in `src/host/windows.ts` through Win32 Credential Management APIs; cover standard operations, absent-target idempotency, target validation, and value non-disclosure in `test/host-contract.test.ts` and a Windows-only `test/windows-credential-manager.int.test.ts`.
- [ ] Add credential-reference persistence and legacy copy/read-back/cryptographic-verify/delete orchestration in `src/auth/tokens.ts`, `src/crypto.ts`, and a focused `src/host/secret-migration.ts`; update `.env.example`, `test/crypto-redaction.test.ts`, `test/oauth-callback.test.ts`, and `test/pilot-security-source.test.ts` to prove failed verification preserves the legacy value and no secret enters DB, logs, files, commands, or browser storage.
- [ ] Run the chunk validation (`npm run verify` plus the standard-user Windows Credential Manager integration test), record artifacts in `.ralph/progress.md`, then append `<promise>CHUNK COMPLETE: CHUNK_1_SECRETS</promise>`.

## CHUNK_2_AUTH — Loopback and SID-bound local owner session

- [ ] Add current-user SID/install-identity primitives to `src/host/types.ts`, `src/host/windows.ts`, and non-secret `%LOCALAPPDATA%\APHub\install.json` handling; add mismatch/refusal coverage to `test/host-contract.test.ts` without weakening tenant or role authorization.
- [ ] Implement one-time bootstrap nonce issuance/consumption and `POST /api/local-session/bootstrap` in `src/auth/local-session.ts`, `src/auth/session.ts`, `src/http.ts`, and `app/api/local-session/bootstrap/route.ts`; prove expiry, replay, SID mismatch, HttpOnly/host-only/SameSite=Strict cookie attributes, and lockout in `test/auth-session.test.ts` and `test/local-session.test.ts`.
- [ ] Centralize loopback Host, forwarded-host, Origin, and CSRF enforcement in `src/auth/guard.ts`/`src/http.ts`; bind backend and `web:start` explicitly to `127.0.0.1`, remove Google SSO as the installed-product entry authority in `app/login/page.tsx` and `app/api/auth/**`, and add hostile HTTP plus Playwright coverage in `test/http-security.test.ts`, `test/auth-guard.test.ts`, and `e2e/app.spec.ts`.
- [ ] Run `npm run verify` plus `Get-NetTCPConnection` listener inspection and local bootstrap E2E, preserve RBAC/cross-tenant evidence, then append `<promise>CHUNK COMPLETE: CHUNK_2_AUTH</promise>` to `.ralph/progress.md`.

## CHUNK_3_GMAIL — Desktop PKCE loopback authorization

- [ ] Replace fixed web-client authorization assumptions with a Desktop OAuth `S256` PKCE transaction in `src/auth/gmail-oauth.ts`, `src/auth/connect-state.ts`, and `src/auth/connect-urls.ts`, including random verifier/state, ten-minute expiry, exact dynamic loopback redirect, and occupied-port selection; test cryptographic shape, expiry, replay, wrong verifier, and non-loopback refusal in `test/connect-state.test.ts`, `test/connect-urls.test.ts`, and `test/oauth-callback.test.ts`.
- [ ] Implement the temporary `127.0.0.1:{dynamicPort}` callback listener and update `POST /api/connections/gmail/start` in `src/services/action/connections.ts` and `app/api/connections/gmail/start/route.ts`; exchange tokens directly into CHUNK_1 credential targets while PostgreSQL retains references/non-secret scope metadata, with failure/cleanup coverage in `test/connections-start.test.ts` and `test/oauth-callback.test.ts`.
- [ ] Rewire `src/gmail/client.ts`, `src/gmail/adapter.ts`, `src/ingest/poll-cycle.ts`, and `src/gmail/drafts.ts` to resolve credentials only through `SecretStore`; retain watched-label effective-once attachment/classification/statement behavior and draft create/update/discard without send, verified by `test/infra-ingest.test.ts`, `test/bank-statements.test.ts`, `test/gmail-drafts.test.ts`, `test/reply-drafts-api.test.ts`, and `test/lockdown.test.ts`.
- [ ] Run `npm run verify` and the disposable-mailbox live trace (authorization, ≤5-minute poll, one document outcome, source-thread unsent draft, zero send capability), record artifacts, then append `<promise>CHUNK COMPLETE: CHUNK_3_GMAIL</promise>`.

## CHUNK_4_TRANSPORTS — Shared QBO transport registry

- [ ] Define `QuickBooksTransport`, `CompanyBinding`, verified capabilities, idempotency lookup, `postBill`, and read-back contracts in `src/accounting/contracts.ts`/`src/connectors/types.ts`; add transport registry selection in `src/connectors/factory.ts` using `connections.transport_mode`, and enforce that `src/pipeline/posting.ts` remains provider-neutral with `test/architecture-connector-path.test.ts` and `scripts/lint-noleak.mjs`.
- [ ] Adapt current QBO direct mode to `direct_local_oauth` in `src/connectors/qbo.ts`, `src/qbo/client.ts`, `src/qbo/write.ts`, and `src/auth/qbo-oauth.ts`, using localhost authorization where supported and Credential Manager tokens; implement visible `REDIRECT_MODE_UNSUPPORTED` recovery without a public relay and test company identity, owner gate, ambiguity adoption, and reconciliation in `test/posting.test.ts`, `test/qbo-token-refresh.test.ts`, and `test/oauth-callback.test.ts`.
- [ ] Implement authenticated `api_adapter` and fixed-allowlist `mcp_adapter` transports under `src/connectors/adapters/`, with bounded payload/output/schema validation and no arbitrary tool calls; add `POST /api/connections/quickbooks`, `POST /api/connections/quickbooks/[id]/authorize`, and `GET /api/connections/quickbooks/[id]/capabilities` through `src/services/action/connections.ts`, `src/services/read/providerCapabilities.ts`, and matching `app/api/connections/quickbooks/**` routes.
- [ ] Build a shared fixture-driven parity suite in `test/connector-contract.test.ts` and hostile adapter coverage in `test/qbo-adapter-contract.test.ts` proving every mode preserves approval, proof, owner gate, exact company, idempotency, timeout adoption, read-back, reconciliation, audit, RBAC, and tenant isolation.
- [ ] Run `npm run verify` plus disposable direct/adapter transport certification where configured, retain identity/post/read-back artifacts, then append `<promise>CHUNK COMPLETE: CHUNK_4_TRANSPORTS</promise>`.

## CHUNK_5_DESKTOP — Local durable QuickBooks Desktop

- [ ] Reconcile `src/qbdesktop/index.ts`, `src/qbdesktop/soap.ts`, and `src/qbdesktop/qwc.ts` with CHUNK_2 loopback/SID ownership and CHUNK_1 QBWC credential targets; prove `/qbwc` is local-only and no QBD payload crosses a hosted relay in `test/qbdesktop.test.ts`, `test/http-security.test.ts`, and `test/architecture-connector-path.test.ts`.
- [ ] Adapt `src/connectors/qbd.ts`, `src/qbdesktop/production.ts`, and `src/qbdesktop/durable-jobs.ts` to the shared `QuickBooksTransport` guarantees while retaining tenant/connection/company-bound leasing, default-off process and durable owner gates, and restart-safe job state; cover restart boundaries and cross-company/cross-tenant refusal in `test/provider-durable-jobs.test.ts` and `test/qbd-posting-contract.test.ts`.
- [ ] Preserve BillAdd uncertainty handling and require authoritative BillQuery read-back before atomic posting/reconciliation/audit finalization in `src/qbdesktop/qbxml.ts` and `src/qbdesktop/production.ts`; test malformed responses, expired leases, lost responses, read-back mismatch, adoption without BillAdd replay, and rollback injection in `test/qbd-posting-contract.test.ts`.
- [ ] Run `npm run verify` plus a disposable QBD Pro/Premier/Enterprise QBWC certification as configured, retain company/lease/BillQuery/reconciliation artifacts, then append `<promise>CHUNK COMPLETE: CHUNK_5_DESKTOP</promise>`.

## CHUNK_6_WATCHDOG — Standard-user continuous operation

- [ ] Make `%LOCALAPPDATA%\APHub` and current-user ACLs the canonical install target in `deploy/install-core.ps1`, `deploy/install-gui.ps1`, `pilot/install-pilot.ps1`, and `pilot/New-PilotCredentialBundle.ps1`; remove hosted URL/secret-file requirements, record SID/install ID/non-secret paths in `install.json`, and validate clean non-admin installation in `pilot/validate-clean-install.ps1` plus `test/pilot-security-source.test.ts`.
- [ ] Replace the current watchdog wiring with a per-user Task Scheduler supervisor in `pilot/start-aphub.ps1`, `pilot/aphub-watchdog.xml`, and `src/host/windows.ts` that sequences PostgreSQL readiness, backend/workers, and UI, enforces loopback arguments, restores killed children within 90 seconds, and stops after five failures in ten minutes with bounded backoff; add deterministic process/scheduler tests in `test/host-contract.test.ts`.
- [ ] Add non-sensitive runtime telemetry and `GET /api/runtime/status` through `src/telemetry.ts`, `src/http.ts`, `src/services/read/runtimeStatus.ts`, `app/api/runtime/status/route.ts`, and a Settings/Today status surface; include process, DB, queue, Gmail, QuickBooks, disk, poll, backup, restart, and credential-store accessibility without values, tested in `test/operational-health.test.ts` and `e2e/app.spec.ts`.
- [ ] Implement local JSON log rotation and Windows toast/event summaries plus wake/network recovery in `src/logger.ts`, `src/host/windows.ts`, `src/ingest/poll-cycle.ts`, and scheduler scripts; prove durable cursor/job resumption, no duplicate replay, 10 MiB/10-file retention, and actionable crash-loop/network/disk/backup warnings in `test/telemetry.test.ts`, `test/provider-durable-jobs.test.ts`, and Windows harness scripts.
- [ ] Run `npm run verify` plus clean-install, sign-in/reboot, child-kill, sleep/wake, network-loss/restore, and log/notification drills under a standard user; retain artifacts and append `<promise>CHUNK COMPLETE: CHUNK_6_WATCHDOG</promise>`.

## CHUNK_7_CERTIFICATION — Installed-product proof and truthful release

- [ ] Run and retain the complete local gate (`npm run lint`, `npm run lint:noleak`, `npm run typecheck`, `npm test`, `npm run web:build`, `npm run test:ui-contract`) plus static no-send/no-public-bind/no-provider-bypass scans; record exact commands, commit, counts, and failures in `docs/CHUNK_7_CERTIFICATION-audit.md`.
- [ ] Execute `pilot/validate-clean-install.ps1` on a clean standard-user Windows profile and capture loopback listeners, local bootstrap, reboot/startup, forced child recovery, bounded crash loop, network loss/resume, credential migration failure rollback, backup/restore, health/status, and no-plaintext evidence under a redacted certification artifact directory.
- [ ] Certify disposable Gmail polling/drafts, QBO direct localhost where supported, every configured authenticated API/MCP adapter, and disposable QBD transport; capture exact company identity, owner approval/gate, idempotency lookup, provider post/read-back, reconciliation, audit, and explicit zero-email-send evidence, leaving any unavailable transport `NOT VERIFIED`.
- [ ] Reconcile `.env.example`, `README.md`, `INSTALL.md`, `docs/OPERATIONS-multi-edition-accounting.md`, `docs/LIVE-VALIDATION-SETUP.md`, installer copy, and UI capability claims with observed results; state localhost limitations, adapter authorization requirements, supported editions, default-off production writes, and any remaining `NOT VERIFIED` items.
- [ ] Run `truth-fix-loop` followed by `spec-vs-build-brutal-audit` against `specs/SPEC-windows-local-only-runtime.md`; require zero P0/P1 local defects and artifact-backed acceptance criteria before appending `<promise>CHUNK COMPLETE: CHUNK_7_CERTIFICATION</promise>`.
