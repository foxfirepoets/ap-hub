{
  "name": "ap-hub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AI Accountant Hub — human-supervised Gmail invoice and statement intake for supported QuickBooks editions",
  "main": "dist-desktop/main.mjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "desktop:build": "node scripts/build-desktop.mjs",
    "desktop:dev": "npm run desktop:build && electron dist-desktop/main.mjs",
    "dist:win": "npm run desktop:build && electron-builder --win --publish never",
    "dist:mac": "npm run desktop:build && electron-builder --mac --publish never",
    "cli": "tsx src/cli.ts",
    "migrate:up": "tsx src/db/migrate.ts up",
    "migrate:down": "tsx src/db/migrate.ts down",
    "db:reset": "tsx src/db/migrate.ts reset",
    "seed": "tsx src/db/seed.ts",
    "test": "vitest run",
    "test:int": "vitest run --mode integration",
    "lint": "eslint \"src/**/*.ts\" \"test/**/*.ts\" \"desktop/**/*.ts\"",
    "lint:noleak": "node scripts/lint-noleak.mjs",
    "verify": "npm run lint && npm run lint:noleak && npm run typecheck && npm test && npm run web:build && npm run test:ui-contract",
    "verify:live": "npm run test:int",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "web:dev": "next dev",
    "web:build": "next build && node scripts/externalize-inline-scripts.mjs",
    "web:lint": "next lint",
    "e2e": "npm run test:ui-contract",
    "test:ui-contract": "npm run desktop:build && playwright test"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "commander": "^12.1.0",
    "dotenv": "^16.4.7",
    "googleapis": "^144.0.0",
    "mupdf": "^1.28.0",
    "next": "^14.2.15",
    "pg": "^8.13.1",
    "pg-boss": "^10.1.5",
    "pino": "^9.5.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@playwright/test": "^1.57.0",
    "@types/node": "^22.10.2",
    "@types/pg": "^8.11.10",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "electron": "^33.4.11",
    "electron-builder": "^25.1.8",
    "esbuild": "^0.28.1",
    "eslint": "^9.17.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.1",
    "vitest": "^2.1.8"
  }
}
